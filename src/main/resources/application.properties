# Database
spring.datasource.url=jdbc:mysql://localhost:3306/service_finder
spring.datasource.username=root
spring.datasource.password=pass@123

# JPA
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# JWT - FIXED: Use longer secret key (32+ characters)
jwt.secret=ServiceFinderSecretKey2024ThatIsVerySecureAndLongEnoughForJWT
jwt.expiration=86400000

# Server
server.port=8080

# NEW: Add CORS configuration to fix 403 error
cors.allowed-origins=http://localhost:3000,http://localhost:8080